# Değerlendirme — ck0410

1. s1=6, s2=3 girilirse döngü kaç kere döner?
   (Özgür için: hiç dönmez — `i <= s2` koşulu (6<=3) başlangıçta zaten yanlış, döngü
   gövdesi bir kez bile çalışmadan biter.)

2. Bu programı ck0260'taki `for (int hak = 3; hak > 0; hak--)` ile karşılaştır — ikisi de
   `for` kullanıyor ama biri artıyor biri azalıyor. Hangi kısım bunu belirliyor?
   (Özgür için: adım kısmı — `i++` artan, `hak--` azalan. Yön koşulla değil adımla
   belirlenir.)

# Değerlendirme — ck0140

1. `==` ile `=` arasındaki fark nedir?
   (Özgür için: `=` bir değişkene değer atar, `==` iki değerin birbirine eşit olup
   olmadığını sorar ve sonucu true/false olur.)

2. "Ersoy" yerine "ERSOY" yazılsa program ne cevap verir, neden?
   (Özgür için: "Yanlış cevap." yazar — string karşılaştırması büyük/küçük harfe
   duyarlıdır, "ERSOY" ile "Ersoy" C# için aynı metin değildir.)

3. if'in altındaki satır çalışmazsa hangi satır çalışır?
   (Özgür için: else'in altındaki satır — bir if-else bloğunda ikisinden yalnızca biri
   çalışır, hiçbir zaman ikisi birden çalışmaz.)

# Değerlendirme — ck0190

1. `puan < 0 || puan > 100` ne zaman true olur?
   (Özgür için: iki koşuldan en az biri doğruysa — puan negatifse VEYA 100'den büyükse.)

2. `||` ile `&&` arasındaki fark ne? (bir sonraki derste && göreceğiz, şimdilik tahmin et)
   (Özgür için: || en az biri doğruysa yeter; && ikisinin de doğru olmasını ister —
   kesin cevabı ck0210'da netleşecek.)

3. Bu derste `{ }` neden zorunlu?
   (Özgür için: if'in altında birden fazla satır (iki WriteLine) çalışacağı için —
   ck0150'de gördüğümüz kural burada da geçerli.)

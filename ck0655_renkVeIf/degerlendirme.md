# Değerlendirme — ck0655

1. Console.BackgroundColor = (ConsoleColor)12; satırı if'ten ÖNCE neden var?
   (Özgür için: kullanıcı sayıyı girene kadar ekranda görünecek başlangıç rengini
   ayarlıyor; if çalışınca renk zaten değişecek.)

2. sayi=10 girilirse hangi renk seçilir?
   (Özgür için: 10 çift olduğu için if dalı çalışır, renk 9 olur — 10 aynı zamanda
   5'in katı da olsa, kod bunu ayrıca kontrol etmiyor, sadece çift/tek bakıyor.)

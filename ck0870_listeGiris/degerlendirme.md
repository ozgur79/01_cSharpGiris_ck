# Değerlendirme — ck0870

1. List<int> ile int[] arasındaki temel fark ne?
   (Özgür için: dizinin boyutu new int[N] ile SABİTLENİR; List'in boyutu
   .Add() çağırdıkça büyür, baştan kaç eleman olacağını bilmen gerekmez.)

2. rakamlar.Count ile bir dizinin .Length'i aynı işi mi görür?
   (Özgür için: evet, ikisi de "kaç eleman var" bilgisini verir — sadece List'te
   Count, dizide Length denir.)

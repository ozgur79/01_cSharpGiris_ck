# Değerlendirme — ck0270

1. -3 sayısı için `kalan` ne çıkar, program "tektir" mi "çifttir" mi der?
   (Özgür için: kalan -1 çıkar, `kalan == 0` yanlış olduğu için else çalışır — "tektir"
   der, doğru sonuç da budur.)

2. Bu program neden bölen olarak sabit 2 kullanıyor, kullanıcıya bölen sormuyor?
   (Özgür için: tek/çift kavramı özel olarak "2'ye bölüm" anlamına gelir — bölen 3 ya da
   5 olsaydı kalan farklı bir şey ifade ederdi, "tektir/çifttir" etiketi anlamsız olurdu.)

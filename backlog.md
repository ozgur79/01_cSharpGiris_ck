# Backlog

Canlı durum. Biten madde silinir, `backlog-log.md`'ye taşınır.

- Ders içeriği henüz başlamadı — Faz 0 (kurulum) tamamlandı, müfredata dokunulmadı.
- `arsiv/01cSharpÇekirdekKodlar.txt` henüz işlenmedi — ilk dersler muhtemelen buradan çıkacak.
- Dikkat: bu makinede `dotnet` SDK kurulu değil, sadece runtime (`Microsoft.NETCore.App`,
  `Microsoft.WindowsDesktop.App` 10.0.5) var. Console derslerini ck kendi çalıştırıp
  doğrulayacaksa (bkz. CLAUDE.md "Bitti" tanımı), SDK kurulumu gerekecek.

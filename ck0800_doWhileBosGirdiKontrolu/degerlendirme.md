# Değerlendirme — ck0800

1. string.IsNullOrWhiteSpace ile girdi == "" arasındaki fark ne?
   (Özgür için: girdi == "" sadece TAMAMEN boş girişi yakalar; IsNullOrWhiteSpace
   sadece boşluk karakterlerinden oluşan girişi de (ör. "   ") geçersiz sayar.)

2. Sadece boşluk ("   ") girilirse girdi == "" kontrolü ne yapar?
   (Özgür için: geçersiz saymaz — "   " boş string değildir, kontrolü GEÇER ve
   kabul edilir, oysa aslında anlamlı bir veri değildir.)

# Değerlendirme — ck0885

1. Console.ReadKey ile Console.ReadLine arasındaki fark ne?
   (Özgür için: ReadLine, kullanıcı Enter'a basana kadar TÜM satırı bekler;
   ReadKey tek bir tuşa basılır basılmaz hemen döner, Enter'a gerek yoktur.)

2. Console.ReadKey(true) içindeki true ne işe yarıyor?
   (Özgür için: "intercept" — tuşu YAKALAR ama ekrana YAZDIRMAZ. false ya da
   parametresiz kullanılsaydı basılan tuş normal şekilde ekranda görünürdü.)

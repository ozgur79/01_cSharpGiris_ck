# Değerlendirme — ck0725

1. Array.Reverse(dizi) diziyi mi değiştiriyor, yoksa ters bir kopya mı döndürüyor?
   (Özgür için: dizinin kendisini değiştiriyor — geriye bir şey döndürmüyor (void),
   `dizi = Array.Reverse(dizi);` yazmak hataya yol açar.)

2. Array.Reverse(dizi) iki kez üst üste çağrılırsa dizi ne olur?
   (Özgür için: başlangıç durumuna geri döner — ilk çağrı ters çevirir, ikinci
   çağrı onu tekrar ters çevirip orijinal sıraya döndürür.)

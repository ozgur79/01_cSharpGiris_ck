// Fazladan ; silinince:
int sayac;
for (sayac = 7; sayac <= 10; sayac = sayac + 1)
    Console.WriteLine(sayac);
Console.ReadKey();
// Çıktı: 7, 8, 9, 10 (dört satır) — WriteLine artık gerçekten döngünün içinde.

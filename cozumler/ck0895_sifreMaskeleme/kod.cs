// Gerçek silme fikri: sifre'nin son karakterini çıkar (sifre = sifre.Substring(0,
// sifre.Length-1)) ve ekranda son yıldızı silmek için "\b \b" (backspace, boşluk,
// tekrar backspace) yazdır. Bu dersin kapsamı dışında bırakıldı — sadece fikir.

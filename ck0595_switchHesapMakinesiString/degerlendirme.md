# Değerlendirme — ck0595

1. double.Parse(Console.ReadLine()) ile Convert.ToDouble(Console.ReadLine()) aynı işi
   mi yapıyor?
   (Özgür için: evet, ikisi de metni double'a çevirir; kaynak kodda double.Parse
   kullanılmış, ck0590'da Convert.ToDouble — ikisi de geçerli, işlev aynı.)

2. Bu dersin ck0590'dan tek farkı ne?
   (Özgür için: secim değişkeninin tipi (string yerine char) ve case değerlerinin
   yazımı (tırnaklı "1" yerine tırnaksız '1') — geri kalan mantık birebir aynı.)
